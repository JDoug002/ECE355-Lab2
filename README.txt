First run the "Receiver.py"
Then run "Sender.py"
"Testing rdt1.0 protocol" should be seen in terminal and application will terminate.

The program is set up so the port and IP address should work on your computer without needing to be changed

Jonathan Douglas